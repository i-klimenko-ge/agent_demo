# Agent Demo

This project contains a minimal demo UI and backend.

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
```

## Running

Start the Flask server:

```bash
python app.py
```

Open your browser at `http://localhost:5000` to see the demo UI.
